// Server component wrapper to export route segment config
export const dynamic = 'force-dynamic'

import CallFollowUpPageClient from './CallFollowUpPageClient'

export default function CallFollowUpPage() {
  return <CallFollowUpPageClient />
}
